{
  "private":"true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build":"tsc && api-extractor run --local --verbose --config packages/package2/api-extractor.json && api-extractor run --local --verbose --config packages/package1/api-extractor.json "
  },
  "dependencies": {
    "@microsoft/api-extractor": "^7.18.19",
    "typescript": "^4.4.4"
  }
}
